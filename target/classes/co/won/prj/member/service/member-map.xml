<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="co.won.prj.member.service.MemberService">
	<select id="memberSelectList" resultType="co.won.prj.member.service.MemberVO">
		SELECT * FROM MEMBER
	</select>
	
	<select id="memberSelect" resultType="co.won.prj.member.service.MemberVO">
		SELECT * FROM MEMBER WHERE ID = #{id}
	</select>
	
	<!-- parameterType이 요청값을 넘기기 위해서 필요하다 select는 없어도 상관없다. -->
	<insert id="memberInsert">
	
	</insert>
	
	<update id="memberUpdate">
	
	</update>
	
	<delete id="memberDelete">
	
	</delete>
	
</mapper>